apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: midori-ops-main
  namespace: midori
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "200m"
spec:
  ingressClassName: nginx
  rules:
    - host: midori-ops-main.k8s-app.fcn.applications
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: midori-ops-main
                port:
                  number: 8080
